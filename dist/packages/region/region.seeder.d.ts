export declare class RegionSeeder {
    /**
     * Run the database seeds.
     */
    static run(): Promise<void>;
    /**
     * Truncate the Regions table.
     */
    static truncate(): Promise<void>;
}
