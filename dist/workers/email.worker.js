"use strict";
// // src/workers/email.worker.ts
// import { Job } from "bullmq";
// import { createWorker } from "../core/utils/worker.factory";
// import { EmailJob } from "../core/interfaces/jobs/email";
Object.defineProperty(exports, "__esModule", { value: true });
// // Define the processor function
// const processor = async (job: Job<EmailJob>) => {
//   const { to, subject, body } = job.data;
//   console.log(`[EmailWorker] Sending email to: ${to}, ${subject}, ${body}`);
//   // Call your email sending service here (e.g., nodemailer or SendGrid)
//   // service calling
//   return true;
// };
// // Create the worker
// createWorker<EmailJob, boolean>("emails", processor);
